// // src/types/express.d.ts

// import { JwtPayload } from "jsonwebtoken";

// declare global {
//   namespace Express {
//     interface Request {
//       userId?: string; // Define the correct type for userId if it's not always a string
//       user?: JwtPayload; // Optionally, if you use user information from JWT
//     }
//   }
// }
